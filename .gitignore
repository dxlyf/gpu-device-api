node_modules/
dist-demo/
*.local
.DS_Store

# 调试产生的截图与临时目录，不入库
examples/*.png
.tmp-*/
.npmrc
# 例外的例外：`.tmp-probe/` 这个探针脚本要入库（它是可复现的测量工具，不是一次性产物）
!.tmp-probe/
.tmp-2a/
.tmp-2b/
.tmp-02/
# 批 02 的原生 API 面探针页要入库：它是「原生在本机有没有这个 API」的可复现证据
!.tmp-02/probe/
.tmp-04/
.tmp-05/
.tmp-06/
.tmp-03/
# 批 07 的临时产物目录（node 脚本、日志）。注意：`.tmp-07/probe/` 下的原生探针页
# **无法**用 `!.tmp-07/probe/` 反选回来 —— git 不会进入被忽略的目录，所以那条写法
# （上面 `.tmp-02/probe/` 那行）其实并不生效。批 07 因此不假装它生效：
# 探针页留在磁盘上供复核，入库的证据是 `test/error-scopes.test.ts`（改动前 33 条运行时失败）。
.tmp-07/
# 批 01 的临时产物目录：`dist` 重建前后的文件哈希清单（用来证明「重建真的改变了产物」）、
# 浏览器验证的 JSON/stdout 抓取。证据本体在提交信息与 `docs/backend-limits.md` 里。
.tmp-01/
# 批 13（WEBGL_multi_draw 探针）的临时产物目录：无头验证的 stdout 抓取、截图等。
# 证据本体是 `examples/webgl-multi-draw-probe.html` / `.ts`（入库）与提交信息。
.tmp-13/
# 批 10（WebGPU MRT 逐位置 colorFormats）的临时产物目录：改前/改后实测记录、原生探针页、
# 无头验证的 stdout 抓取。上面的 `.tmp-*/` 已经忽略了它，这一行只是照前几批的惯例写明归属；
# 入库的证据是 `test/webgpu-mrt-positional-formats.test.ts`（改动前 15 用 14 失败）与提交信息。
.tmp-10/
# 批 14（打包入口拆分）的临时目录：第 8 行的 `.tmp-*/` 已经忽略了它，这里显式反选回目录本身，
# 让**测量脚本与消费方探针**入库（它是可复现的测量工具，不是一次性产物）；
# 构建产物、日志与源码快照仍挡在下面几行之外。
!.tmp-14/
.tmp-14/libdist/
.tmp-14/libmodules/
# A/B 构建对比产物（同一份源码 + 新旧构建方式的逐字节比对）与它用的包消费方探针目录
.tmp-14/abtest/
.tmp-14/pkg-consumer/
.tmp-14/snapshot/
# tsc 试跑产物（`.tmp-14/tsconfig.tscout.json` 的输出，用来校验模块树可消费性）
.tmp-14/tscout/
.tmp-14/*.log
# 测量输出（内嵌完整产物代码，体积大且可重跑）与一次性的源码快照都不入库
.tmp-14/*.json
# 批 12（mipLevelExtent 对 3D 的 depth 减半）的临时产物目录：改动前后的实测记录、
# 「排除 src/entries」的 tsc 检查配置。上面的 `.tmp-*/` 已经忽略了它，这一行只是照前几批的惯例
# 写明归属；入库的证据是 `test/mip-level-extent-3d.test.ts`（改动前 5 用 4 失败）与提交信息。
.tmp-12/
# 批 15（在 tsc 模块树之上补 UMD / ES 单文件包）的临时产物目录：体积探针、.cjs 与 .js 的
# require 对照、真实浏览器探针页、postbuild 对照实验、npm pack 前后比对。上面的 `.tmp-*/`
# 已经忽略了它，这一行只是照前几批的惯例写明归属；入库的证据是 `test/entry-surface-dist.test.ts`
# （7 条 → 9 条强制校验）与 `.tmp-15/RESULT.md` 里的原始数字。
.tmp-15/
# Vite 的依赖预打包缓存（启动 dev server 时生成、可随时重建），不属于源码
.vite/
